# 🏗️ Arquitetura do PrevisãoPro

Este documento descreve a arquitetura detalhada do sistema PrevisãoPro.

## 📑 Índice

1. [Visão Geral](#visão-geral)
2. [Arquitetura em Camadas](#arquitetura-em-camadas)
3. [Fluxo de Dados](#fluxo-de-dados)
4. [Componentes do Sistema](#componentes-do-sistema)
5. [Padrões de Design](#padrões-de-design)
6. [Segurança](#segurança)
7. [Performance](#performance)
8. [Escalabilidade](#escalabilidade)

---

## 🎯 Visão Geral

PrevisãoPro é uma aplicação web full-stack construída com:
- **Frontend**: React 19 + TypeScript (SPA)
- **Backend**: Hono (Cloudflare Workers)
- **Database**: SQLite (Cloudflare D1)

### Arquitetura de Alto Nível

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENTE                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Browser    │  │    Mobile    │  │    Tablet    │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          └──────────────────┴──────────────────┘
                             │
                    ┌────────▼─────────┐
                    │   CDN (Static)   │
                    │   React App      │
                    └────────┬─────────┘
                             │ HTTP/REST
                    ┌────────▼─────────┐
                    │  Cloudflare      │
                    │  Workers (API)   │
                    │  Hono Framework  │
                    └────────┬─────────┘
                             │ SQL
                    ┌────────▼─────────┐
                    │  Cloudflare D1   │
                    │  SQLite          │
                    └──────────────────┘
```

---

## 🏛️ Arquitetura em Camadas

### 1. Camada de Apresentação (Frontend)

```
┌─────────────────────────────────────────────┐
│            PRESENTATION LAYER                │
├─────────────────────────────────────────────┤
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │  Pages   │  │Components│  │  Hooks   │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘ │
│       │             │              │        │
│  ┌────▼─────────────▼──────────────▼─────┐ │
│  │         React Router                  │ │
│  └───────────────────┬───────────────────┘ │
│                      │                      │
│  ┌───────────────────▼───────────────────┐ │
│  │         State Management              │ │
│  │  (React Context + Custom Hooks)       │ │
│  └───────────────────┬───────────────────┘ │
│                      │                      │
│  ┌───────────────────▼───────────────────┐ │
│  │         HTTP Client (fetch)           │ │
│  └───────────────────────────────────────┘ │
│                                              │
└──────────────────────┬───────────────────────┘
                       │ REST API
```

**Responsabilidades:**
- Renderização de UI
- Gerenciamento de estado local
- Validação de formulários
- Navegação entre páginas
- Comunicação com API

**Componentes Principais:**

```
src/react-app/
├── pages/              # Páginas principais
│   ├── Dashboard.tsx
│   ├── Previsoes.tsx
│   ├── Pagamentos.tsx
│   └── Balancetes.tsx
│
├── components/         # Componentes reutilizáveis
│   ├── Layout.tsx
│   ├── KPICard.tsx
│   ├── AlertCard.tsx
│   └── ...
│
└── hooks/              # Custom hooks
    └── useAPI.ts
```

---

### 2. Camada de API (Backend)

```
┌─────────────────────────────────────────────┐
│              API LAYER                       │
├─────────────────────────────────────────────┤
│                                              │
│  ┌──────────────────────────────────────┐  │
│  │         Hono Router                  │  │
│  └────┬─────┬─────┬─────┬─────┬────────┘  │
│       │     │     │     │     │            │
│  ┌────▼─┐ ┌─▼───┐ ┌▼───┐ ┌▼───┐ ┌▼─────┐ │
│  │Prev │ │Pagto│ │Dash│ │Alert│ │Relat │ │
│  │API  │ │API  │ │API │ │API  │ │API   │ │
│  └────┬─┘ └─┬───┘ └┬───┘ └┬───┘ └┬─────┘ │
│       │     │      │      │      │        │
│  ┌────▼─────▼──────▼──────▼──────▼─────┐  │
│  │      Business Logic Layer           │  │
│  │  - Validação (Zod)                  │  │
│  │  - Cálculos                         │  │
│  │  - Transformações                   │  │
│  └────────────────┬─────────────────────┘  │
│                   │                         │
│  ┌────────────────▼─────────────────────┐  │
│  │      Data Access Layer               │  │
│  │  - Queries SQL                       │  │
│  │  - Prepared Statements               │  │
│  └────────────────┬─────────────────────┘  │
│                   │                         │
└───────────────────┼─────────────────────────┘
                    │ SQL
```

**Responsabilidades:**
- Receber requisições HTTP
- Validar dados de entrada
- Executar lógica de negócio
- Acessar banco de dados
- Retornar respostas formatadas

**Estrutura de APIs:**

```
src/worker/
├── index.ts              # Entry point + Router
├── previsao-api.ts       # API de previsões
├── pagamentos-api.ts     # API de pagamentos
├── dashboard-api.ts      # API de dashboard
├── alertas-api.ts        # API de alertas
├── relatorios-api.ts     # API de relatórios
└── document-templates.ts # Templates de documentos
```

---

### 3. Camada de Dados (Database)

```
┌─────────────────────────────────────────────┐
│            DATA LAYER                        │
├─────────────────────────────────────────────┤
│                                              │
│  ┌──────────────────────────────────────┐  │
│  │      SQLite (Cloudflare D1)          │  │
│  ├──────────────────────────────────────┤  │
│  │                                       │  │
│  │  Core Tables:                        │  │
│  │  ├─ condominios                      │  │
│  │  ├─ competencias                     │  │
│  │  ├─ previsao_itens                   │  │
│  │  ├─ centros_custo                    │  │
│  │  └─ centro_custo_itens               │  │
│  │                                       │  │
│  │  Feature Tables:                     │  │
│  │  ├─ pagamentos_efetivados            │  │
│  │  ├─ despesas_extras                  │  │
│  │  ├─ alertas                          │  │
│  │  └─ historico_comparacao             │  │
│  │                                       │  │
│  │  Support Tables:                     │  │
│  │  ├─ categorias                       │  │
│  │  └─ auditoria_previsao               │  │
│  │                                       │  │
│  └──────────────────────────────────────┘  │
│                                              │
└──────────────────────────────────────────────┘
```

**Responsabilidades:**
- Armazenar dados persistentes
- Garantir integridade referencial
- Indexar para performance
- Executar queries SQL

---

## 🔄 Fluxo de Dados

### Fluxo de Leitura (Query)

```
┌─────────┐     1. Request      ┌─────────┐
│         │ ──────────────────> │         │
│ Browser │                     │   API   │
│         │ <────────────────── │         │
└─────────┘     6. Response     └────┬────┘
                                     │
                              2. Validate
                                     │
                              ┌──────▼──────┐
                              │  Business   │
                              │   Logic     │
                              └──────┬──────┘
                                     │
                              3. Query DB
                                     │
                              ┌──────▼──────┐
                              │  Database   │
                              └──────┬──────┘
                                     │
                              4. Return Data
                                     │
                              ┌──────▼──────┐
                              │  Transform  │
                              └──────┬──────┘
                                     │
                              5. Format JSON
```

### Fluxo de Escrita (Mutation)

```
┌─────────┐   1. Submit Form    ┌─────────┐
│         │ ──────────────────> │         │
│ Browser │                     │   API   │
│         │ <────────────────── │         │
└─────────┘   8. Confirmation   └────┬────┘
                                     │
                              2. Validate
                                     │
                              ┌──────▼──────┐
                              │  Business   │
                              │   Logic     │
                              └──────┬──────┘
                                     │
                              3. Calculate
                                     │
                              ┌──────▼──────┐
                              │  Database   │
                              │ Transaction │
                              └──────┬──────┘
                                     │
                        4. Insert/Update/Delete
                                     │
                              5. Trigger Audit
                                     │
                              6. Generate Alerts
                                     │
                              7. Return Success
```

### Exemplo: Criar Previsão

```
Frontend (Previsoes.tsx)
  │
  ├─> User preenche formulário
  │
  ├─> handleSubmit()
  │
  ├─> apiPost('/api/previsoes/salvar', dados)
  │
  └─> await response
      │
      ▼
Backend (previsao-api.ts)
  │
  ├─> POST /api/previsoes/salvar
  │
  ├─> Validate com Zod
  │
  ├─> Iniciar transaction
  │
  ├─> Inserir/atualizar itens
  │
  ├─> Calcular totais
  │
  ├─> Atualizar competencia
  │
  ├─> Commit transaction
  │
  └─> Return success
      │
      ▼
Database (D1)
  │
  ├─> INSERT INTO previsao_itens
  │
  ├─> UPDATE competencias
  │
  ├─> INSERT INTO auditoria
  │
  └─> Commit
```

---

## 🧩 Componentes do Sistema

### 1. Sistema de Previsões

**Propósito:** Gerenciar previsões de custos mensais

**Componentes:**
```
┌─────────────────────────────────────┐
│      Sistema de Previsões           │
├─────────────────────────────────────┤
│                                      │
│  ┌────────────────────────────┐    │
│  │  Previsoes.tsx (Page)      │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  PrevisaoForm (Component)  │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  RealtimeCalculator        │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  DocumentGenerator         │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  previsao-api.ts (Backend) │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  Database Tables:          │    │
│  │  - competencias            │    │
│  │  - previsao_itens          │    │
│  │  - centro_custo_itens      │    │
│  └────────────────────────────┘    │
│                                      │
└──────────────────────────────────────┘
```

**Fluxo Principal:**
1. Usuário cria competência (mês/ano)
2. Adiciona itens de despesa por categoria
3. Sistema calcula automaticamente
4. Usuário visualiza em tempo real
5. Pode gerar documentos
6. Fecha competência quando aprovado

---

### 2. Sistema de Pagamentos

**Propósito:** Controlar pagamentos efetivados

**Componentes:**
```
┌─────────────────────────────────────┐
│     Sistema de Pagamentos           │
├─────────────────────────────────────┤
│                                      │
│  ┌────────────────────────────┐    │
│  │  Pagamentos.tsx (Page)     │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  Lista de Pagamentos       │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  Comparação Gráfica        │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  pagamentos-api.ts         │    │
│  └───────┬────────────────────┘    │
│          │                          │
│  ┌───────▼────────────────────┐    │
│  │  Database Tables:          │    │
│  │  - pagamentos_efetivados   │    │
│  │  - despesas_extras         │    │
│  └────────────────────────────┘    │
│                                      │
└──────────────────────────────────────┘
```

**Fluxo Principal:**
1. Sistema gera pagamentos da previsão
2. Usuário marca como pago quando efetivado
3. Registra valor real (pode diferir)
4. Sistema calcula variações
5. Gera alertas automáticos
6. Exibe comparação visual

---

### 3. Sistema de Documentos

**Propósito:** Gerar documentos profissionais

**Componentes:**
```
┌──────────────────────────────────────┐
│      Sistema de Documentos           │
├──────────────────────────────────────┤
│                                       │
│  ┌─────────────────────────────┐    │
│  │  DocumentGenerator          │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  Seleção de Tipo            │    │
│  │  - Previsão Condomínio      │    │
│  │  - Previsão Centro Custo    │    │
│  │  - Fatura                   │    │
│  │  - Balancete                │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  Seleção de Ações           │    │
│  │  - Visualizar               │    │
│  │  - Nova Aba                 │    │
│  │  - Imprimir                 │    │
│  │  - Download                 │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  DocumentPreview (Modal)    │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  document-templates.ts      │    │
│  │  - BASE_STYLES              │    │
│  │  - gerarDocumentoX()        │    │
│  └─────────────────────────────┘    │
│                                       │
└───────────────────────────────────────┘
```

---

### 4. Sistema de Alertas

**Propósito:** Notificar usuário sobre eventos importantes

**Componentes:**
```
┌──────────────────────────────────────┐
│       Sistema de Alertas             │
├──────────────────────────────────────┤
│                                       │
│  ┌─────────────────────────────┐    │
│  │  AlertsWidget (Header)      │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  Detector de Eventos        │    │
│  │  - Variação > 10%           │    │
│  │  - Pagamento atrasado       │    │
│  │  - Despesa não aprovada     │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  alertas-api.ts             │    │
│  │  - Gerar alertas            │    │
│  │  - Classificar severidade   │    │
│  └────────┬────────────────────┘    │
│           │                          │
│  ┌────────▼────────────────────┐    │
│  │  Database Table:            │    │
│  │  - alertas                  │    │
│  └─────────────────────────────┘    │
│                                       │
└───────────────────────────────────────┘
```

---

## 🎨 Padrões de Design

### 1. Component Pattern (Frontend)

**Compound Components:**
```typescript
// Componente principal com subcomponentes
<KPICard>
  <KPICard.Header icon={Icon} />
  <KPICard.Value value="100" />
  <KPICard.Trend data={[...]} />
</KPICard>
```

**Render Props:**
```typescript
<DataProvider url="/api/data">
  {({ data, loading, error }) => (
    <div>...</div>
  )}
</DataProvider>
```

**Custom Hooks:**
```typescript
function usePrevisao(id: number) {
  const { data, loading, error } = useAPI(`/api/previsoes/${id}`);
  const [calculated, setCalculated] = useState();
  
  useEffect(() => {
    if (data) {
      setCalculated(calculate(data));
    }
  }, [data]);
  
  return { data, calculated, loading, error };
}
```

---

### 2. Repository Pattern (Backend)

```typescript
// Abstração de acesso a dados
class PrevisaoRepository {
  constructor(private db: D1Database) {}
  
  async findById(id: number): Promise<Competencia | null> {
    return await this.db
      .prepare('SELECT * FROM competencias WHERE id = ?')
      .bind(id)
      .first();
  }
  
  async save(data: Competencia): Promise<void> {
    // ...
  }
}
```

---

### 3. Service Pattern (Business Logic)

```typescript
// Lógica de negócio isolada
class PrevisaoService {
  constructor(
    private repo: PrevisaoRepository,
    private calculator: CalculatorService
  ) {}
  
  async createPrevisao(data: CreatePrevisaoDTO): Promise<Competencia> {
    // Validar
    this.validate(data);
    
    // Calcular
    const calculated = this.calculator.calculate(data);
    
    // Salvar
    return await this.repo.save(calculated);
  }
}
```

---

### 4. Factory Pattern (Document Generation)

```typescript
// Factory para criar documentos
class DocumentFactory {
  static create(type: DocumentType): DocumentGenerator {
    switch (type) {
      case 'condominio':
        return new CondominioDocumentGenerator();
      case 'centro_custo':
        return new CentroCustoDocumentGenerator();
      case 'fatura':
        return new FaturaDocumentGenerator();
      default:
        throw new Error('Unknown document type');
    }
  }
}
```

---

## 🔒 Segurança

### 1. Autenticação (Planejado)

```
┌─────────┐     1. Login        ┌─────────┐
│         │ ──────────────────> │         │
│ Client  │                     │   API   │
│         │ <────────────────── │         │
└─────────┘  2. JWT Token       └─────────┘
      │
      ├─> Armazenar em localStorage
      │
      └─> Incluir em headers:
          Authorization: Bearer <token>
```

### 2. Autorização (Planejado)

**Roles:**
- `admin` - Acesso total
- `sindico` - Gestão do condomínio
- `contador` - Visualização e relatórios
- `inquilino` - Visualização do seu centro de custo

**Permissions:**
```typescript
interface Permission {
  resource: string;  // 'previsoes', 'pagamentos'
  actions: string[]; // ['create', 'read', 'update', 'delete']
}
```

### 3. Validação de Dados

**Input Validation (Zod):**
```typescript
const PrevisaoSchema = z.object({
  mes: z.number().min(1).max(12),
  ano: z.number().min(2024),
  area_total_m2: z.number().positive(),
  acrescimo_percentual: z.number().min(0).max(100)
});
```

**SQL Injection Prevention:**
```typescript
// ✅ Sempre usar prepared statements
db.prepare('SELECT * FROM users WHERE id = ?').bind(userId);

// ❌ Nunca concatenar strings
db.prepare(`SELECT * FROM users WHERE id = ${userId}`);
```

### 4. CORS

```typescript
app.use('*', cors({
  origin: ['https://app.previsaopro.com'],
  methods: ['GET', 'POST', 'PUT', 'DELETE'],
  allowedHeaders: ['Content-Type', 'Authorization'],
  credentials: true
}));
```

---

## ⚡ Performance

### 1. Database Optimization

**Índices:**
```sql
-- Sempre indexar FKs
CREATE INDEX idx_competencias_condominio ON competencias(condominio_id);

-- Indexar campos de filtro
CREATE INDEX idx_pagamentos_status ON pagamentos_efetivados(status);

-- Índices compostos para queries frequentes
CREATE INDEX idx_itens_comp_cat ON previsao_itens(competencia_id, categoria);
```

**Query Optimization:**
```typescript
// ✅ Buscar apenas campos necessários
SELECT id, nome, area_m2 FROM centros_custo

// ❌ Evitar SELECT *
SELECT * FROM centros_custo
```

---

### 2. Frontend Optimization

**Code Splitting:**
```typescript
// Lazy load de páginas
const Pagamentos = lazy(() => import('./pages/Pagamentos'));
```

**Memoization:**
```typescript
// Memorizar cálculos pesados
const calculatedValue = useMemo(
  () => expensiveCalculation(data),
  [data]
);

// Memorizar callbacks
const handleClick = useCallback(
  () => doSomething(param),
  [param]
);
```

**Debouncing:**
```typescript
// Debounce de buscas
const debouncedSearch = useMemo(
  () => debounce((term: string) => search(term), 300),
  []
);
```

---

### 3. Caching Strategy

```
┌─────────┐
│ Browser │
│  Cache  │  ← Static assets (CSS, JS, images)
└────┬────┘    Cache-Control: max-age=31536000
     │
     ▼
┌─────────┐
│   CDN   │  ← Edge caching
│  Cache  │    Cache-Control: s-maxage=3600
└────┬────┘
     │
     ▼
┌─────────┐
│  API    │  ← API response caching
│  Cache  │    ETag, Last-Modified
└────┬────┘
     │
     ▼
┌─────────┐
│Database │  ← Query result caching
│  Cache  │    In-memory cache
└─────────┘
```

---

## 📈 Escalabilidade

### 1. Horizontal Scaling

```
           ┌─────────────┐
           │   CDN       │
           │ (Worldwide) │
           └──────┬──────┘
                  │
      ┌───────────┴───────────┐
      │                       │
┌─────▼─────┐           ┌─────▼─────┐
│ Worker 1  │           │ Worker 2  │
│ (US-East) │           │ (EU-West) │
└─────┬─────┘           └─────┬─────┘
      │                       │
      └───────────┬───────────┘
                  │
           ┌──────▼──────┐
           │  Database   │
           │  (Primary)  │
           └─────────────┘
```

### 2. Data Partitioning (Future)

**Por Condomínio:**
```
DB_Condominio_1 → Condomínios 1-1000
DB_Condominio_2 → Condomínios 1001-2000
DB_Condominio_3 → Condomínios 2001-3000
```

**Por Data:**
```
DB_2024 → Competências de 2024
DB_2025 → Competências de 2025
DB_2026 → Competências de 2026
```

---

### 3. Read Replicas (Future)

```
           ┌─────────────┐
           │  Primary    │
           │  (Write)    │
           └──────┬──────┘
                  │ Replication
      ┌───────────┼───────────┐
      │           │           │
┌─────▼─────┐ ┌──▼──────┐ ┌──▼──────┐
│ Replica 1 │ │Replica 2│ │Replica 3│
│  (Read)   │ │ (Read)  │ │ (Read)  │
└───────────┘ └─────────┘ └─────────┘
```

---

## 🔄 Deployment Architecture

### Production Setup

```
┌──────────────────────────────────────────────┐
│              PRODUCTION                       │
├──────────────────────────────────────────────┤
│                                               │
│  ┌────────────────────────────────────┐     │
│  │         Cloudflare CDN              │     │
│  │    (Global Edge Network)            │     │
│  └─────────────┬───────────────────────┘     │
│                │                              │
│  ┌─────────────▼───────────────────────┐     │
│  │      Static Assets (Frontend)       │     │
│  │      - React SPA                    │     │
│  │      - Cached at edge               │     │
│  └─────────────┬───────────────────────┘     │
│                │                              │
│  ┌─────────────▼───────────────────────┐     │
│  │   Cloudflare Workers (Backend)      │     │
│  │   - Hono API                        │     │
│  │   - Edge compute                    │     │
│  └─────────────┬───────────────────────┘     │
│                │                              │
│  ┌─────────────▼───────────────────────┐     │
│  │      Cloudflare D1 (Database)       │     │
│  │      - SQLite                       │     │
│  │      - Globally replicated          │     │
│  └─────────────────────────────────────┘     │
│                                               │
└───────────────────────────────────────────────┘
```

---

## 📊 Monitoring & Logging (Planejado)

```
┌─────────┐
│   App   │
└────┬────┘
     │
     ├─> Logs ────────────> Log Aggregation
     │                      (e.g., Logtail)
     │
     ├─> Metrics ─────────> Metrics Service
     │                      (e.g., Grafana)
     │
     ├─> Errors ──────────> Error Tracking
     │                      (e.g., Sentry)
     │
     └─> Analytics ───────> Analytics
                            (e.g., PostHog)
```

---

## 🎯 Conclusão

A arquitetura do PrevisãoPro foi projetada para ser:

✅ **Escalável** - Pode crescer horizontalmente
✅ **Performática** - Otimizada em todas as camadas
✅ **Segura** - Múltiplas camadas de segurança
✅ **Manutenível** - Código limpo e documentado
✅ **Testável** - Separação clara de responsabilidades
✅ **Extensível** - Fácil adicionar novas features

---

**Última atualização:** 2025-01-18
**Versão:** 2.0.0
